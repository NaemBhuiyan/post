import { ADD_CATEGORY } from "./actionTypes";

catagoriesReducer = (state = initialState, action) => {
  switch () {
    case ADD_CATEGORY:
      break;

    default:
      break;
  }
};

export default catagoriesReducer;
